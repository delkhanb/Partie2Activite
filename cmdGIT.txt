cmd git:

git init  --> initialisation git

git add .  --> ajouter a l'index git

git commit -m "message" --> commiter les modification avec un message pour se retrouver apres

git -a -m "message" --> commiter et mettre ajour 

git log --> afficher les log git

git push origin master --> envoyer son repo sur le repo origine